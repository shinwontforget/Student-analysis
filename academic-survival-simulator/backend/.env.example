# Internal API Security Secret — must match frontend's INTERNAL_API_SECRET
INTERNAL_API_SECRET=your-shared-internal-api-secret-key

# Server settings (Must be bound to 127.0.0.1 for internal access only)
HOST=127.0.0.1
PORT=8000
